# eslint-config-fuzion

Fuzion Eslint config with:

- Airbnb config
- Typescript
- Prettier

## Usage

```
npx install-peerdeps --dev @fuzionchain/eslint-config-fuzion
```

Add `"extends": "@fuzionchain/eslint-config-fuzion"` to your eslint config file.
